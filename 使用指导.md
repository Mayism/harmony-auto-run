# `config_and_build.py` 使用指导

> HarmonyOS 工程 **配置 → 编译 → 部署 → 启动** 一站式自动化脚本。

---

## 目录

- [1. 概述](#1-概述)
- [2. 使用前提](#2-使用前提)
  - [2.1 工具链要求](#21-工具链要求)
  - [2.2 环境变量配置](#22-环境变量配置)
  - [2.3 设备要求](#23-设备要求)
  - [2.4 配置文件 config.yaml](#24-配置文件-configyaml)
- [3. 使用方式](#3-使用方式)
  - [3.1 基本用法](#31-基本用法)
  - [3.2 命令行参数](#32-命令行参数)
  - [3.3 用法示例](#33-用法示例)
- [4. 执行流程（6 步）](#4-执行流程6-步)
- [5. 常见问题](#5-常见问题)

---

## 1. 概述

该脚本将 HarmonyOS 开发中重复的 **配置 → 编译 → 签名 → 部署 → 启动** 流程整合为一条命令，主要完成：

1. 读取 `config.yaml` 配置文件（`signingConfigs` + `bundleName`）
2. 自动写入 `AppScope/app.json5`（bundleName + 补齐必填字段）
3. 自动写入 `build-profile.json5`（signingConfigs）
4. 调用 `ohpm install` + `hvigorw assembleHap` 编译工程
5. 自动查找签名后的 `.hap` 并 `hdc install` 部署到设备
6. 通过 `hdc shell aa start` 启动应用

---

## 2. 使用前提

### 2.1 工具链要求

| 工具 | 用途 | 通常来源 |
|---|---|---|
| `hdc` | 设备通信、安装 HAP、启动应用 | DevEco Studio 附带，路径如 `D:/Huawei/DevEcoStudio/sdk/default/openharmony/toolchains/hdc.exe` |
| `hvigorw` | HarmonyOS 官方构建 | DevEco Studio 附带，路径如 `D:/Huawei/DevEcoStudio/tools/hvigor/bin/hvigorw.bat` |
| `node` | hvigor 运行依赖 | 需 Node.js 16+ |
| `java` (JDK) | hvigor 运行依赖 | 需 JDK 17+ |
| `ohpm` | 鸿蒙包管理器 | DevEco Studio 附带，路径如 `D:/Huawei/DevEcoStudio/tools/ohpm/bin/ohpm.bat` |
| Python 3.10+ | 运行本脚本 | 无需额外第三方依赖 |

### 2.2 环境变量配置

环境变量为**可选**，未设置时脚本会尝试从系统 `PATH` 和常见候选目录中自动查找。**推荐在系统环境变量中配置以下变量**：

#### 2.2.1 核心变量

| 环境变量 | 作用 | 示例值 (Windows) |
|---|---|---|
| `HVIGOR_HOME` | hvigor 安装根目录 | `D:\Huawei\DevEcoStudio\tools\hvigor` |
| `HDC_PATH` | hdc 可执行文件完整路径 | `D:\Huawei\DevEcoStudio\sdk\default\openharmony\toolchains\hdc.exe` |
| `NODE_HOME` | Node.js 安装根目录 | `C:\Program Files\nodejs` |
| `JAVA_HOME` | JDK 安装根目录 | `C:\Program Files\Java\jdk-17` |
| `OHPM_HOME` | ohpm 安装根目录 | `D:\Huawei\DevEcoStudio\tools\ohpm` |

#### 2.2.2 Windows 环境变量配置方法

**方法一：系统环境变量（推荐）**

1. 右键 "此电脑" → "属性" → "高级系统设置" → "环境变量"
2. 在 "系统变量" 区域，点击 "新建" 逐一添加上表中变量
3. 如需直接在终端执行 `java`、`node`、`ohpm`、`hdc`、`hvigorw`，可把对应工具目录或 `bin` 目录加入系统 `Path`
4. 重启终端使配置生效

**方法二：命令行临时设置（当前终端有效）**

```cmd
set HVIGOR_HOME=D:\Huawei\DevEcoStudio\tools\hvigor
set HDC_PATH=D:\Huawei\DevEcoStudio\sdk\default\openharmony\toolchains\hdc.exe
set NODE_HOME=C:\Program Files\nodejs
set JAVA_HOME=C:\Program Files\Java\jdk-17
set OHPM_HOME=D:\Huawei\DevEcoStudio\tools\ohpm
```

**方法三：PowerShell 临时设置**

```powershell
$env:HVIGOR_HOME = "D:\Huawei\DevEcoStudio\tools\hvigor"
$env:HDC_PATH = "D:\Huawei\DevEcoStudio\sdk\default\openharmony\toolchains\hdc.exe"
$env:NODE_HOME = "C:\Program Files\nodejs"
$env:JAVA_HOME = "C:\Program Files\Java\jdk-17"
$env:OHPM_HOME = "D:\Huawei\DevEcoStudio\tools\ohpm"
```

#### 2.2.3 环境变量查找优先级

脚本按以下顺序查找工具，取第一个命中：

1. 检查对应的环境变量（如 `HDC_PATH`、`HVIGOR_HOME`），若为文件路径直接使用，若为目录则在该目录下查找工具文件
2. 从系统 `PATH` 中查找（`shutil.which`）
3. 从候选 `bin/` 子目录查找（`${HVIGOR_HOME}/bin`、`${NODE_HOME}/bin`、`${JAVA_HOME}/bin`、`${OHPM_HOME}/bin` 等）
4. 对 `hdc`、`hvigorw` 等关键工具，找不到完整路径时会兜底使用工具名本身（依赖调用时 cwd 或 `PATH` 能定位到）

#### 2.2.4 验证环境配置

配置完成后，在终端中验证工具是否可被找到：

```cmd
:: 验证 hdc
hdc version

:: 验证 Node.js
node --version

:: 验证 JDK
java -version

:: 验证 ohpm
ohpm -v

:: 验证 hvigorw
hvigorw -v
```

> 也可以先执行 `python config_and_build.py --project D:/MyApp --dry-run`，脚本开头会打印出解析到的所有工具路径。

### 2.3 设备要求

- 一台通过 USB（或无线）连接的 HarmonyOS / OpenHarmony 真机
- 设备已开启 **开发者模式** 和 **USB 调试**
- 在终端执行 `hdc list targets` 可看到设备序列号

### 2.4 配置文件 config.yaml

脚本默认读取当前目录下的 `config.yaml`，其中包含应用签名信息和包名。也可以通过 `--yaml` 传入其他配置文件。当前脚本使用内置逻辑解析，不依赖 `pyyaml`；其中 `signingConfigs` 必须写成 JSON 数组格式（以 `signingConfigs: [` 开始），`bundleName` 支持带引号或不带引号。

```yaml
# config.yaml
signingConfigs: [
  {
    "name": "default",
    "type": "HarmonyOS",
    "material": {
      "storeFile": "D:/keys/myapp.p12",
      "storePassword": "000000xxxx",
      "certpath": "D:/keys/myapp.cer",
      "profile": "D:/keys/myappDebug.p7b",
      "signAlg": "SHA256withECDSA",
      "keyAlias": "myapp",
      "keyPassword": "000000xxxx"
    }
  }
]

bundleName: com.example.myapp
```

**必填字段**：

| 字段 | 说明 |
|---|---|
| `bundleName` | 应用包名，格式 `com.xxx.xxx` |
| `signingConfigs` | 签名配置数组，通常含一组 HarmonyOS 签名材料，必须是可被 `json.loads` 解析的 JSON 数组 |

> `signingConfigs` 的字段内容遵从 `build-profile.json5` 的 `app.signingConfigs` 字段规范，脚本会原样写入该位置。

---

## 3. 使用方式

### 3.1 基本用法

```bash
python config_and_build.py --project D:/MyHarmonyApp
```

脚本依次执行：配置写入 → ohpm install → hvigor 编译 → 查找 HAP → hdc 安装 → hdc 启动。

### 3.2 命令行参数

| 参数 | 简写 | 必填 | 说明 |
|---|---|---|---|
| `--yaml` | `-y` | 否 | 配置文件路径，默认 `./config.yaml`（见 [2.4](#24-配置文件-configyaml)） |
| `--project` | `-p` | 是 | 鸿蒙工程根目录（含 `build-profile.json5` 的目录） |
| `--dry-run` | — | 否 | 跳过实际编译、安装、启动；配置写入仍会执行，除非同时加 `--skip-config` |
| `--skip-config` | — | 否 | 跳过配置文件的写入步骤 |
| `--skip-build` | — | 否 | 跳过 ohpm install + hvigor 编译步骤 |

### 3.3 用法示例

#### 完整流程

```bash
# 配置 + 编译 + 安装 + 启动，一条命令搞定
python config_and_build.py -p D:\MyHarmonyApp
```

#### 预览编译/安装/启动（dry-run）

```bash
# 会写入 app.json5 / build-profile.json5，但跳过编译、安装、启动
python config_and_build.py -p D:\MyHarmonyApp --dry-run
```

#### 纯预览（不写配置、不编译、不安装、不启动）

```bash
# 只解析工具路径和配置，并打印后续命令
python config_and_build.py -p D:\MyHarmonyApp --dry-run --skip-config
```

> 当前脚本没有“只编译但不安装/启动”的独立参数；不加 `--dry-run` 时，编译成功后会继续查找 HAP、安装并启动。

#### 使用其他配置文件

```bash
# 默认使用 ./config.yaml；只有需要切换配置时才传 --yaml
python config_and_build.py -y ./other-config.yaml -p D:\MyHarmonyApp
```

#### 已配好，只编译 + 部署 + 启动

```bash
# app.json5 / build-profile.json5 已手动配好，跳过配置写入
python config_and_build.py -p D:\MyHarmonyApp --skip-config
```

#### 已有 HAP，只安装 + 启动

```bash
# 跳过配置和编译，直接用已有 HAP 装到设备
python config_and_build.py -p D:\MyHarmonyApp --skip-config --skip-build
```

---

## 4. 执行流程（6 步）

| 步骤 | 说明 |
|---|---|
| **[1/6] 配置 app.json5** | 将配置文件中的 `bundleName` 写入 `AppScope/app.json5`，并自动补齐 `label`、`icon`、`vendor`、`versionCode`、`versionName` 等必填字段 |
| **[2/6] 配置 signingConfigs** | 将配置文件中的 `signingConfigs` 写入 `build-profile.json5` 的 `app.signingConfigs` |
| **[3/6] 编译** | 先执行 `ohpm install` 安装依赖，再执行 `hvigorw assembleHap` 编译（超时 10 分钟） |
| **[4/6] 查找 HAP** | 遍历工程目录，优先找 `*-signed.hap`，其次找任意 `.hap`，取最新文件 |
| **[5/6] 安装** | 先执行 `hdc install -r <hap>` 安装到设备，再通过 `hdc shell bm dump -n <bundleName>` 校验设备端是否真的安装成功；如果安装失败或命令返回成功但设备端查不到包，会按需执行 `hdc uninstall <bundleName>` 后重试安装 |
| **[6/6] 启动** | 先尝试通过 `hdc shell bm dump -n <bundleName>` 解析 Ability 名，解析不到时默认使用 `EntryAbility`，再执行 `hdc shell aa start -a <ability> -b <bundleName>` |

---

## 5. 常见问题

### Q1：报错 "配置缺少字段: signingConfigs"

配置文件缺少 `signingConfigs` 字段，或没有按 `signingConfigs: [` 的 JSON 数组格式书写。请按 [2.4](#24-配置文件-configyaml) 格式补充。

### Q2：报错 "未找到 hvigorw 构建脚本"

`HVIGOR_HOME` 环境变量未设置，且 `PATH` 中也没有 `hvigorw.bat`。解决方案：

- 设置 `HVIGOR_HOME` 指向 Deveco Studio 的 `tools/hvigor` 目录
- 或者将该目录加入系统 `PATH`

### Q3：报错 "编译超时（>10 分钟）"

编译过程超过 10 分钟被自动终止。可能原因：
- 首次编译下载依赖较慢，可重试一次
- 网络问题导致依赖拉取阻塞
- 工程过大导致编译本身耗时

### Q4：报错 "未检测到已连接设备"

`hdc list targets` 返回空。请检查：
- 设备是否通过 USB 连接
- 设备是否开启开发者模式和 USB 调试
- 终端执行 `hdc list targets` 确认能看到设备

### Q5：同包名应用安装失败

脚本会在首次 `hdc install -r <hap>` 失败后检查设备上是否已安装同 `bundleName` 应用。如果确认是同包名应用冲突，会自动执行：

```bash
hdc uninstall <bundleName>
hdc install -r <hap>
```

如果卸载失败或重试安装仍失败，脚本会打印 hdc 输出并终止。

### Q6：显示安装指令成功但设备上没有应用

脚本不会只相信 `hdc install` 的返回码。安装命令返回成功后，会轮询执行：

```bash
hdc shell bm dump -n <bundleName>
```

如果一段时间内仍查不到包，会自动清理并重试安装一次；重试后仍查不到包时，脚本会报错终止，不会继续启动应用。

### Q7：启动失败 / 非默认 Ability 名

脚本会先尝试从 `hdc shell bm dump -n <bundleName>` 解析启动 Ability，解析不到时使用 `EntryAbility` 作为兜底。如果你的应用主 Ability 仍启动失败：

- 如果仍不对，可手动执行 `hdc shell aa start -a <YourAbility> -b <bundleName>` 启动
